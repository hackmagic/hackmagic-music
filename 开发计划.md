目标：面向 AI 时代的现代播放器
界面:GPUI+gpui-component，要实现全部国际化和多主题风格。
多音频引擎切换支持:BASS、FFMPEG、MCI、Rodio、symphonia。
使用 Tokio 处理高并发的音频流解码、AI 推理任务和文件 I/O，绝不阻塞 UI 线程。
AI 推理引擎：candle 或 ort。candle 是 Hugging Face 推出的轻量级 Rust ML 框架，支持 GPU 加速且无需庞大的 Python 依赖；ort 是 ONNX Runtime 的 Rust 绑定，适合运行标准化的量化模型。
本地数据与检索：lancedb。 lancedb 是专为本地和边缘计算设计的向量数据库，Rust 原生，支持将歌曲的音频特征、歌词文本转化为向量，实现毫秒级语义搜索。

---

## 架构审查：不合理与需要调整的问题

以下基于当前代码（2026-07-23）对照开发计划目标进行审查，按严重程度排列。

### 🔴 严重问题（影响核心目标达成）

1. **AI 推理管线完全未接入播放器**
   - `ai.rs` 和 `vecdb.rs` 是独立模块，在 `Player`、`MusicPlayer` 和任何播放流程中**从未被导入或调用**。
   - 播放歌曲时不会自动提取 embedding，媒体库扫描不会触发向量索引，`VecDb` 没有任何数据写入。
   - 后果：开发计划目标"AI 推理引擎"和"本地向量检索"目前是空中楼阁，只有骨架代码，对用户无实际价值。

2. **缺少 `AiEngine` 结构体，与开发计划描述不符**
   - 计划中描述"`AiEngine` 结构体 + `load_bytes()` 骨架"，但实际代码只有 `pub fn extract_embedding()` 裸函数，没有 `AiEngine` 结构体，没有模型加载路径，没有 `forward()` 推理。

3. **`extract_embedding()` 计算量大且无异步包装**
   - 对一首 3 分钟 44100Hz 立体声歌曲，`extract_embedding()` 会计算约 258 帧 FFT（1024 点/帧，512 hop），每帧有 O(n_fft·log n_fft) 复杂度的 FFT + O(n_mels·n_bins) 的 mel 滤波矩阵乘。
   - 该函数是同步阻塞的，没有 `spawn_blocking` 包装——如果从播放流程中调用，会直接阻塞 UI 线程或 tokio 工作线程。
   - 全局 `FftPlanner` 的 `Mutex` 还会序列化所有并发 embedding 计算。

4. **`vecdb.rs` 错误类型与项目不一致**
   - 全项目使用 `crate::error::Result<T>` + `PlayerError`，但 `VecDb` 所有方法返回 `Result<(), Box<dyn std::error::Error>>`。集成时会导致类型不匹配。

5. **VecDb 缺少歌词语义搜索所需字段**
   - 计划提到"歌词语义搜索"，但 `VecDb` 的 schema 只有 `path` 和 `embedding` 两列，没有 `title`、`artist`、`album`、`lyrics`、`lyrics_embedding` 等元数据。
   - 没有歌词 embedding 模块（NLP 文本 embedding 模型），无法实现歌词语义搜索。

### 🟠 中等问题（架构设计缺陷）

6. **`PlayerEngine` trait 抽象泄漏，引擎能力差异大**
   - `set_pitch()`、`set_equalizer()`、`set_reverb()`、`fft_data()`、`preload_next()`、`crossfade_out()` 等方法在大多数引擎中都是空操作（no-op），但 trait 要求所有引擎实现。
   - `MciEngine` 不支持 speed/pitch/EQ/reverb/FFT：`set_speed()` 是空操作，`set_pitch()` 是空操作，`fft_data()` 返回空 vec。
   - `FfmpegEngine` 的 `set_speed()` 只存值不生效，`set_pitch()` 是空操作，`set_equalizer()` 是空操作。
   - 后果：用户切换到 MCI 或 FFmpeg 引擎时，变速、变调、均衡器、频谱可视化等功能**静默失效**，没有任何 UI 提示。

7. **`RodioEngine` 和 `SymphoniaEngine` 几乎完全重复**
   - 两个引擎的 `SymphoniaSource`/`RodioSource` 结构体、Iterator 实现、`rodio::Source` 实现、结构体字段、`new()` 方法等几乎逐行相同。
   - 差异仅在解码器 (`rodio::Decoder` vs `symphonia::`)，但输出端都依赖 `rodio::Sink` + `rodio::OutputStream`。
   - 大量重复代码增加了维护成本和编译时间。

8. **`Player` 结构体字段锁粒度过细**
   - `eq_gains`、`eq_enabled`、`reverb_mix`、`reverb_time`、`reverb_enabled`、`ab_repeat`、`spectrum_columns`、`fft_size`、`fft_data`、`spectrum_peaks`、`display_format`、`volume_map`、`cue_end_pos` 各自是一个 `Mutex`。
   - 13 个独立的 Mutex，其中大部分只在少数方法中访问。建议合并为 `Mutex<PlayerSettings>` 结构体，减少锁数量和管理复杂度。

9. **`EngineStatus` 快照与 GUI 字段的双重拷贝**
   - `poll_player_state_inner()` 既从 `EngineStatus`（arc-swap 快照）读取曲目信息，又直接从引擎读取 position/duration/volume/state。
   - GUI 的 `self.title`、`self.artist`、`self.album` 是快照的副本，但 `self.position`、`self.duration`、`self.is_playing` 是独立读取的。
   - 这两个来源可能不同步，导致显示不一致（例如曲目已切换但 position 还是旧值）。

10. **`unsafe impl Send/Sync for Player` 缺乏验证**
    - `Player` 包含 `Box<dyn PlayerEngine>` 和 `Mutex<Playlist>` 等字段，编译器无法自动推导 Send/Sync。
    - 代码使用 `unsafe impl` 绕过编译器检查，但如果某个引擎实现不是真正的 Send/Sync，会导致未定义行为。
    - 建议：通过确保所有字段都是 Send + Sync 来让编译器自动推导，或添加运行时断言。

### 🟡 需要关注的问题

11. **无缝切换（gapless）仅 BASS 引擎支持**
    - `preload_next()` 只在 `BassEngine` 中真实实现（mmap 预加载 + BASS stream 预创建）。
    - 其他引擎的 `preload_next()` 是空操作但 `Player::play_at_index()` 仍会调用它，造成虚假的"预加载完成"假象。

12. **`Config::PlayConfig::engine` 选项未更新**
    - 描述中写引擎选项为 `"bass", "mci", "ffmpeg"`，但 `EngineType::from_str()` 已支持 `"rodio"` 和 `"symphonia"`。
    - 配置的默认值、文档注释和 UI 中的引擎选择下拉框均未同步更新。

13. **媒体库扫描不触发 AI embedding 提取**
    - `MusicPlayer::new()` 中的自动扫描逻辑（`scan_directory`）只填充 JSON 缓存，不调用 `extract_embedding()` 或 `VecDb::index_track()`。
    - 即使未来将 AI 管线接入播放器，已有曲库也不会被索引。

14. **`render()` 方法 454 行，`render_menu_bar()` 1120 行**
    - 两个巨型方法严重违反单一职责原则，难以维护、测试和调试。
    - 建议：将菜单栏拆分为按功能分类的子渲染方法（文件菜单、播放菜单、视图菜单等）。

15. **`symphonia_engine` 实际依赖 rodio 输出**
    - `SymphoniaEngine` 使用 `rodio::Sink` + `rodio::OutputStream` 播放，并非纯 symphonia 管线。
    - 这违背了"纯 Rust 解码器，支持更多格式"的初衷——如果 rodio 有 bug，symphonia 引擎同样受影响。

16. **全局 FFT Planner 的 Mutex 持有时间过长**
    - `fft_planner()` 返回的 `Mutex<FftPlanner>` 在整个 `extract_embedding()` 调用期间被持有（数百次 FFT 计算），而不是只用于 `plan_fft_forward()`。
    - 建议：使用 `std::thread_local!` 存储 `FftPlanner`，或将 `plan_fft_forward` 移出锁外。

17. **缺少 AI 管线集成测试**
    - `ai.rs` 有单元测试，`vecdb.rs` 有单元测试，但没有测试完整的 `decode → extract_embedding → VecDb::index_track → VecDb::search` 流程。

18. **未支持 ort（ONNX Runtime）**
    - 开发计划明确提到"candle 或 ort"，但只有 candle 实现（且是 stub），没有 ort 相关代码或 feature flag。

### 📋 建议改进顺序

| 优先级 | 改进项 | 工作量估计 |
|--------|--------|-----------|
| P0 | 将 AI/Vector 管线接入 Player 播放流程 | 3-5 天 |
| P0 | 实现 `AiEngine` 结构体 + 真实模型加载 | 5-10 天 |
| P1 | 统一 VecDb 错误类型为 `crate::error::Result` | 0.5 天 |
| P1 | 扩展 VecDb schema 加入歌词 + 元数据字段 | 1 天 |
| P1 | 为引擎能力差异做运行时检测和 UI 降级提示 | 2-3 天 |
| P2 | 合并 RodioEngine 和 SymphoniaEngine 公共代码 | 1-2 天 |
| P2 | 合并 Player 中细粒度 Mutex 为 `PlayerSettings` 结构体 | 1 天 |
| P2 | 消除 EngineStatus 与 GUI 字段的双重状态 | 2 天 |
| P2 | 为 `extract_embedding` 添加 `spawn_blocking` 包装 | 0.5 天 |
| P3 | 拆分 render_menu_bar 巨型方法 | 2-3 天 |
| P3 | 添加 AI 管线集成测试 | 1 天 |
| P3 | 添加 ort feature flag 支持 | 3-5 天 |

# 开发进度监控

## 一、多音频引擎（BASS / FFmpeg / MCI / Rodio / symphonia）

| 引擎 | 状态 | 说明 |
|------|------|------|
| BassEngine | ✅ | 使用 libloading + BASS DLL，功能完整 |
| FfmpegEngine | ✅ | FFmpeg 子进程 + cpal 回放 |
| MciEngine | ✅ | Windows 原生 mciSendStringW，新加 |
| RodioEngine | ✅ | 纯 Rust 解码（mp3/flac/vorbis），新加 |
| symphonia | ✅ | 纯 Rust 解码器，支持更多格式（已修复编译错误） |
| EngineType enum | ✅ | Bass/Mci/Ffmpeg/Rodio/Symphonia |
| Player::new() 引擎选择 | ✅ | 根据配置选择引擎 |

**待办：**
- [ ] symphonia 引擎实现（参考 rodio_engine.rs 结构）

---

## 二、Tokio 异步非阻塞

| 子项 | 状态 | 说明 |
|------|------|------|
| Tokio 依赖 | ✅ | Cargo.toml `features = ["full"]` |
| arc-swap 锁无关快照 | ✅ | `ArcSwap<EngineStatus>` 字段 |
| EngineStatus 结构体 | ✅ | state/pos/dur/vol/speed/曲目/spectrum/peaks/fft |
| refresh_status() 发布快照 | ✅ | 各操作后调用 |
| GUI poll 从快照读 track info | ✅ | 避免每帧 playlist 锁 |
| GUI render 从快照读 spectrum | ✅ | 避免每帧 FFT 重算 |
| GUI 播放操作包进 std::thread::spawn | ✅ | 16 处 play_at_index/next/prev |
| 音频 decode 移到 spawn_blocking | ✅ | `engine.open()` → `runtime().spawn_blocking` |
| Tokio runtime 初始化 | ✅ | 全局 `runtime()` fn via OnceLock |
| `tokio::spawn_blocking` 替代裸 `std::thread::spawn` | ✅ | 26 处全部替换 |

**待办：**
- （无）

---

## 三、GPUI 界面（i18n / 多主题 / 布局）

| 子项 | 状态 | 说明 |
|------|------|------|
| GPUI 0.2.2 + gpui-component 0.5.1 | ✅ | 框架已集成 |
| 响应式布局（BIG/NARROW/SMALL/MINI） | ✅ | 4 种布局模式 |
| 播放控制栏 | ✅ | 上一曲/播放/暂停/下一曲/停止/进度条/音量 |
| 均衡器面板 | ✅ | 10 段 EQ + preset |
| 频谱可视化 | ✅ | 对数/线性 + 峰值衰减 |
| 桌面歌词 | ✅ | 卡拉 OK 样式 |
| 歌词编辑器 | ✅ | 时间轴编辑 |
| 歌词下载 | ✅ | 搜索引擎 + 自动匹配 |
| 播放列表管理 | ✅ | 排序/搜索/收藏/多版本 |
| 媒体库浏览器 | ✅ | 按目录/歌手/专辑浏览 |
| 文件浏览器 | ✅ | 目录树 + 文件列表 |
| SMTC 集成 | ✅ | Windows 媒体控制 |
| 托盘图标 | ✅ | 系统托盘菜单 |
| 主题系统（7 套） | ✅ | Default/Ocean/Forest/Lavender/Sunset/Midnight/Autumn/Spring |
| 国际化（en/zh） | ✅ | 全部 UI 文案已翻译，0 残留硬编码 |
| 迷你模式布局 | ✅ | Mini 模式播放控制 |
| 浮动播放列表 | ✅ | 独立窗口 |

**待办：**
- （无）

---

## 四、AI 推理引擎（candle）

| 子项 | 状态 | 说明 |
|------|------|------|
| candle-core / candle-nn 依赖 | ✅ | Cargo.toml |
| extract_embedding() | ✅ | 真实 FFT mel 谱 → 64 mel 频带 × 4 时间段 = 256 维 |
| mel_filterbank | ✅ | 三角滤波器组 + Hann 窗 |
| 余弦相似度验证 | ✅ | test_similar_inputs_similar_embeddings 验证 440Hz vs 442Hz vs 噪声 |
| 模型加载 (CLAP/music2vec) | 🔄 | load_bytes() 骨架，待接真实 forward 推理 |
| GPU 加速 | ⬜ | 待 candle-core cuda feature |

**待办：**
- [ ] 集成真实模型，替换 mel 谱嵌入
- [ ] GPU 加速

---

## 五、本地向量检索（lancedb）

| 子项 | 状态 | 说明 |
|------|------|------|
| lancedb 依赖 | ✅ | Cargo.toml, arrow-array/schema 58 |
| VecDb 模块 | ✅ | open_db / ensure_table / index_track / remove_track / search |
| 向量 Schema | ✅ | path:Utf8 + embedding:FixedSizeList(F32, 256) |
| 邻近搜索 | ✅ | L2 距离, limit k, 返回 SearchHit |
| 集成测试 | ✅ | index_and_search + remove 2 测试通过 |

**待办：**
- [ ] 接入播放器 pipeline（decode → extract_embedding → index_track）
- [ ] 添加 Metadata 列（title/artist/album）实现混合检索

---

## 六、本轮已完成（2026-07 会话）

### 6.1 RodioEngine
- 纯 Rust 解码（rodio 0.19 mp3/flac/vorbis）
- 自定义 RodioSource 更新 AtomicU64 位置
- speed/pitch/eq/reverb 接口
- FFT 从解码缓冲区计算

### 6.2 MciEngine
- Windows mciSendStringW 原始 FFI
- 命令：open/play/pause/resume/stop/seek/volume

### 6.3 arc-swap 锁无关快照
- `ArcSwap<EngineStatus>` 替代 Mutex 路径
- `refresh_status()` 发布 18 字段快照
- GUI poll/render 从快照读 track info + spectrum/peaks

### 6.4 GUI 播放操作异步化
- 16 处 play_at_index/next/prev 包进 `std::thread::spawn`
- 音频 decode 不再阻塞 UI 主线程

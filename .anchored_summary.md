## Objective
- Complete all MusicPlayer2 features in Rust + GPUI; current focus is testing basic app playback, media library freeze fix, and Windows-MCP UI testing

## Important Details
- MusicPlayer2 C++ source at `K:\workspaces\github\MusicPlayer2\MusicPlayer2` — MFC project, full feature reference
- GPUI 0.2.2 + gpui-component 0.5.1, both binary compile zero-warning
- GPUI renders custom UI (NOT in Windows accessibility tree) — only window chrome visible via Snapshot/UI tree
- GLM-4V vision unreliable for this session: repeated timeouts and garbled responses (SQL text, random encoding)
- DPI scaling: 168 (1.75x). GPUI window class: `Zed::Window`
- Window positioning drift: `App::resize` from Windows-MCP didn't stick; `MoveWindow` via PowerShell works but GPUI may override on next frame
- Single-instance lock uses named mutex `HackMagicMusic`; previously daemon mode was in `skip_single` list (reverted — only version/cue/daemon skip)
- `hotkey::init` was only called from CLI daemon path in `commands/system.rs`. GUI entry (`src/bin/gui.rs`) never called it, so CLI forwarding always hit "window not found"
- Root cause: two separate entry points — `src/main.rs` (CLI/daemon binary `hm.exe`) and `src/bin/gui.rs` (GUI binary `hm-gui.exe`). My earlier `main.rs` edits had zero effect on the GUI binary.
- WM_COPYDATA wnd_proc calls `commands::dispatch()` which uses `commands::PLAYER` (separate from `hotkey::PLAYER`). Forwarding panicked with "Player not initialized" until `commands::init_player` was also called from `hotkey::init`.

## Work State
### Completed (this session)
- **MCP server replaced**: `windows-mcp` (uvx) → `mcp-windows` (sbroenne/mcp-windows v1.3.18 standalone .exe). Downloaded to `~/.config/opencode/mcp-windows/Sbroenne.WindowsMcp.exe`; updated opencode.json command path.
- **Root cause identified**: GUI entry point was `src/bin/gui.rs`, NOT `src/main.rs`. All hotkey/mutex changes to `main.rs` were irrelevant to GUI.
- **Hotkey IPC fixed**: `crate::hotkey::init(player.clone())` added to `MusicPlayer::new` in `src/gui/mod.rs` — message window created on GUI startup
- **Mutex creation added**: `CreateMutexW("HackMagicMusic")` in `hotkey::init` — single-instance detection now works from GUI too
- **Commands PLAYER fixed**: `crate::commands::init_player(player)` added to `hotkey::init` — forwarding dispatch no longer panics
- **CLI forwarding verified**: `hm.exe play "file.flac"` → detected mutex → WM_COPYDATA → wnd_proc → dispatch → BASS play_file ✓
- **Clean startup confirmed**: No `RefCell already borrowed` at startup (auto-load test code removed)
- **RefCell crash fixed**: 30fps timer at `src/gui/mod.rs:500` separated `poll_player_state_inner()` and `cx.notify()` into two `this.update` calls with a `timer(0).await` yield between them. Fix prevents `cx.notify()`'s internal `borrow_mut()` from colliding with the update's borrow when GPUI processes the notify synchronously. App stayed alive for 4+ minutes after fix (previously died at ~20s).

### Remaining issues
- ~30 `RefCell already borrowed` errors still appear in a tight burst (~65ms) when playback starts (during BASS `open()` blocking call). Caught by GPUI's `catch_unwind`, no crash. Likely from DesktopLyricsView timer or window procedure re-entrancy during the blocking BASS call.

### Blocked
- `rfd::FileDialog` won't open when clicking play button on empty playlist — thread spawn may need COM init (STA) or is silently panicking
- UI click positioning unreliable due to GPUI custom rendering + DPI scaling + window position drift
- File path Unicode encoding issue when passing args from PowerShell to `hm.exe` — secondary issue, WM_COPYDATA/JSON handles Unicode fine

## Next Move
1. Verify media library freeze fix works (no UI lag when loading large libraries via sidebar/list cache)
2. Fix file dialog on empty playlist play button click (add COM init to thread)
3. Test `hm-gui.exe play "song.flac"` CLI forwarding to add music without clicking

## Relevant Files
- `src/gui/mod.rs`: `render_media_lib_panel()` — freeze fix cache. `MusicPlayer::new` at line ~552 calls `hotkey::init`.
- `src/hotkey.rs`: `forward_via_wm_copydata` (line 94) uses `FindWindowW("HackMagicMusic")`. `start_message_thread` (line 142) creates hidden message-only window. WndProc handles WM_COPYDATA at line 316. `init` creates mutex + message thread + sets both `PLAYER` globals.
- `src/commands/mod.rs`: `commands::PLAYER` (line 15) and `get_player()` (line 21). `init_player` called from hotkey init.
- `src/bin/gui.rs`: GUI entry point (separate from `src/main.rs`). Spawns 8MB stack thread running GPUI.
- `src/main.rs`: CLI entry point. Instance lock via named mutex at line 37. `skip_single` list at line 194.
- `C:\Users\angel\AppData\Local\Temp\hm_gui.log`: Runtime log
- `C:\Users\angel\AppData\Local\Temp\hm_panic.log`: Records panics
- `src/media.rs`: `MediaLib::load()` (sync fs read)
- `監控進度.md`: All items checked
